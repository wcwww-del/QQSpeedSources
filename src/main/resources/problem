1.关于track_record数据表是不是该分成两张表，一张A车国服一张T车国服，这两张表都与track_map的id连接起来，
同时track_record也要与car的id连接起来（先不搞，后期优化）


2.前端报文中不需要输入创建时间和更新时间，是不是在实体类中可以忽略掉（已完成）


3.查询、删除单个条目时，对控制层接收的参数进行调整，添加DTO层接收前端传入的数据（已完成）


4.数据表中datetime类型的数据与实体类中LocalDateTime类型的数据一致，都改成date类型（已完成）


5.数据库表中sort属性有什么用？是不是可以去掉？按上线时间或者星级进行排序即可(已完成，关于上线时间的数据类型问题参考问题5)


6.后台管理部分的controller要加上管理员权限（重要）


7.给每个数据库添加一个新字段：操作人，也在MyBatis自动填充字段中填充,后端查询操作添加操作人查询（重要）


8.前台分页查询，采用上架时间查询时可以采取区间查询，其余参数查询可以多选查询


9.分页查询时不需要返回全部信息，只返回精简信息即可，用户点击具体条目时展开所有具体信息（id、操作人等信息除外）


10.修改信息部分，使用的方法是先删除原来信息然后添加新信息，会导致创建时间改变，最好优化一下


11.添加部分日志方便查问题


11.后端完成赛车部分（完成前述优化之后），其余部分服务测试之前完成













